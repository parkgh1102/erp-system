# Dependencies
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local
.env.postgres
# Keep .env.production and .env.example for deployment reference

# Security and sensitive files
*.key
*.pem
*.cert
*.crt
*.p12
*.pfx
secret*
secrets/
config/secrets/
ssl/
keys/

# Production builds
# 앞의 '/'는 루트만 매치해 frontend/dist·backend/dist가 추적되던 문제가 있었음.
# 'dist/'는 모든 하위 경로의 dist 디렉터리를 매치(배포는 전부 소스에서 새로 빌드함).
dist/
/build

# Database files
*.db
*.sqlite
*.sqlite3
database/
backup/
backups/
dumps/

# Log files
logs/
*.log

# Runtime data
pids/
*.pid
*.seed
*.pid.lock

# Coverage directory used by tools like istanbul
coverage/
*.lcov

# Dependency directories
jspm_packages/

# Optional npm cache directory
.npm

# Optional eslint cache
.eslintcache

# Output of 'npm pack'
*.tgz

# Yarn Integrity file
.yarn-integrity

# dotenv environment variables file
.env.test

# Stores VSCode versions used for testing VSCode extensions
.vscode-test

# yarn v2
.yarn/cache
.yarn/unplugged
.yarn/build-state.yml
.yarn/install-state.gz
.pnp.*

# IDE
.vscode/
.idea/
*.swp
*.swo

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# Test screenshots
screenshot*.png

# Uploads and user data
uploads/
temp/
storage/

# Security logs and monitoring
security.log
audit.log
access.log
error.log

# Configuration backups
*.backup
*.bak
*.orig

# Docker secrets (if used)
docker-compose.override.yml
docker-compose.prod.yml

# AWS Elastic Beanstalk
.elasticbeanstalk/*
!.elasticbeanstalk/*.cfg.yml
!.elasticbeanstalk/*.global.yml
*.zip
backend-deploy.zip
*-deploy.zip

# AWS CLI
.aws/

# Terraform (if used)
.terraform/
*.tfstate
*.tfstate.*
.terraform.lock.hcl

# CDK (if used)
cdk.out/

# Amplify
amplify/
.amplifyrc

# Azure
.azure/
*.azurewebsites.net
azure-pipelines.yml
.env.azure
backend-deploy-*.zip
frontend-deploy-*.zip

# Azure CLI
.azure-cli/*.db-shm
*.db-wal
azure-logs/
*.zip
*.cwd
tmpclaude-*

# Windows null device files
nul

# Claude Code auto-generated activity log (Stop hook)
.claude/activity-log.md
